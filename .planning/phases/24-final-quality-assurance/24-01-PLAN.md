---
phase: 24-final-quality-assurance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true

must_haves:
  truths:
    - "All unit tests pass (0 failures)"
    - "All integration tests pass (0 failures)"
    - "All E2E tests pass (0 failures)"
    - "No flaky tests (consistent results on re-run)"
  artifacts:
    - path: "lib/job/generation-count.test.ts"
      provides: "Unit tests for generation count logic"
    - path: "lib/db/__tests__/job-queries.test.ts"
      provides: "Integration tests for database queries"
    - path: "lib/queue/__tests__/retry-strategies.test.ts"
      provides: "Integration tests for retry logic"
    - path: "e2e/job-workflow.spec.ts"
      provides: "E2E tests for job workflow"
  key_links:
    - from: "vitest.config.ts"
      to: "lib/**/*.test.ts"
      via: "test file discovery"
      pattern: "include.*\\*\\*.*test\\.ts"
    - from: "playwright.config.ts"
      to: "e2e/**/*.spec.ts"
      via: "E2E test discovery"
      pattern: "testDir.*e2e"
---

<objective>
Run and verify all automated tests pass (QUAL-01, QUAL-02, QUAL-03)

Purpose: Execute the complete automated test suite in CI mode to verify technical correctness before manual UAT. This establishes the quality gate that all automated tests must pass with zero failures and no flaky behavior.

Output: Test execution results confirming all unit, integration, and E2E tests pass.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-final-quality-assurance/24-RESEARCH.md

Key configuration files:
@vitest.config.ts
@playwright.config.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run unit and integration tests</name>
  <files>None (verification only)</files>
  <action>
Execute the Vitest test suite in CI mode (non-watch):

1. Run `npm run test` which executes `vitest run`
2. Capture the test output including:
   - Total tests run
   - Passed/failed counts
   - Any error messages or stack traces
3. If any tests fail:
   - Document the failing test name and error
   - Identify if it's a code bug or test issue
   - Note: Do NOT fix issues in this plan - document for investigation

Expected test files:
- lib/job/generation-count.test.ts (unit tests)
- lib/db/__tests__/job-queries.test.ts (integration tests)
- lib/queue/__tests__/retry-strategies.test.ts (integration tests)

Important: Tests must pass with zero failures. Any failure blocks QUAL-01/QUAL-02 requirements.
  </action>
  <verify>
Run `npm run test` and confirm:
- Exit code is 0
- All tests pass (green output)
- No skipped or pending tests
  </verify>
  <done>All unit and integration tests pass with zero failures</done>
</task>

<task type="auto">
  <name>Task 2: Run E2E tests</name>
  <files>None (verification only)</files>
  <action>
Execute the Playwright E2E test suite:

1. Ensure dev server is not already running on port 3000 (Playwright auto-starts)
2. Run `npm run test:e2e` which executes `playwright test`
3. Capture the test output including:
   - Total tests run
   - Passed/failed counts
   - Browser used (should be Chromium)
   - Any error messages or screenshots

Expected test file:
- e2e/job-workflow.spec.ts (3 tests: page load, login interface, invalid password)

If tests fail:
- Check if webServer started successfully (port 3000)
- Review Playwright traces in playwright-report/
- Document failures for investigation

Note: Playwright is configured with:
- 30 second timeout per test
- Auto retry on CI (2 retries)
- HTML reporter for results
  </action>
  <verify>
Run `npm run test:e2e` and confirm:
- Exit code is 0
- All 3 E2E tests pass
- Playwright report shows no failures
  </verify>
  <done>All E2E tests pass with zero failures</done>
</task>

<task type="auto">
  <name>Task 3: Verify test consistency (flakiness check)</name>
  <files>None (verification only)</files>
  <action>
Re-run the complete test suite to verify no flaky tests:

1. Run unit/integration tests again: `npm run test`
2. Run E2E tests again: `npm run test:e2e`

Compare results with Task 1 and Task 2:
- Same tests should pass/fail consistently
- No tests that passed before should fail now
- No tests that failed before should suddenly pass

If any flaky behavior detected:
- Document the flaky test name
- Note whether it's timing-related, race condition, or external dependency
- Flag as blocker for production readiness

Quality gate criteria:
- 100% consistency between runs
- No intermittent failures
  </action>
  <verify>
Second run of both test suites produces identical pass/fail results as first run:
- `npm run test` passes again
- `npm run test:e2e` passes again
- No new failures or unexpected passes
  </verify>
  <done>Test suite is consistent with no flaky tests detected</done>
</task>

</tasks>

<verification>
- `npm run test` passes with 0 failures (QUAL-01, QUAL-02)
- `npm run test:e2e` passes with 0 failures (QUAL-03)
- Re-run produces identical results (no flaky tests)
- All quality gates satisfied
</verification>

<success_criteria>
- All unit tests pass (QUAL-01 satisfied)
- All integration tests pass (QUAL-02 satisfied)
- All E2E tests pass (QUAL-03 satisfied)
- No flaky or inconsistent test behavior
- Ready for manual UAT (QUAL-04)
</success_criteria>

<output>
After completion, create `.planning/phases/24-final-quality-assurance/24-01-SUMMARY.md`
</output>
