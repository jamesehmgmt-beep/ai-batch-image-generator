---
phase: 22-codebase-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/ai/gemini.ts
  - lib/ai/gemini-parser.ts
  - lib/ai/gemini-parser.test.ts
  - package.json
autonomous: true

must_haves:
  truths:
    - "No Gemini-related files exist in lib/ai/"
    - "No @google/generative-ai dependency in package.json"
    - "TypeScript compiles without errors"
    - "Existing tests pass"
  artifacts:
    - path: "lib/ai/claude-parser.ts"
      provides: "Active AI parser implementation"
    - path: "lib/ai/anthropic.ts"
      provides: "Active Anthropic client"
  key_links:
    - from: "app/api/ai/parse/route.ts"
      to: "lib/ai/claude-parser.ts"
      via: "import parseJobWithClaude"
      pattern: "parseJobWithClaude"
---

<objective>
Remove all Gemini-related files and dependencies after Claude migration

Purpose: Phase 21 migrated AI parsing from Gemini to Claude Sonnet 4.5. The Gemini files (gemini.ts, gemini-parser.ts, gemini-parser.test.ts) are now dead code. The @google/generative-ai package is no longer used and should be removed.

Output: Clean lib/ai/ directory with only Claude/Anthropic files, reduced package.json dependencies
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-codebase-cleanup/22-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Gemini files</name>
  <files>
    lib/ai/gemini.ts
    lib/ai/gemini-parser.ts
    lib/ai/gemini-parser.test.ts
  </files>
  <action>
Delete the three Gemini-related files:
- lib/ai/gemini.ts (Gemini SDK wrapper with retry logic)
- lib/ai/gemini-parser.ts (Gemini-based prompt parser - replaced by claude-parser.ts)
- lib/ai/gemini-parser.test.ts (Tests for the Gemini parser)

These files are confirmed unused:
- app/api/ai/parse/route.ts imports from claude-parser.ts, not gemini-parser.ts
- No other files import from these modules (verified via grep)
  </action>
  <verify>
Run `ls lib/ai/` to confirm only these files remain:
- anthropic.ts
- claude-parser.ts
- schemas/ (directory)

Run `npm run build` to verify no import errors.
  </verify>
  <done>Gemini files deleted, TypeScript compiles without missing module errors</done>
</task>

<task type="auto">
  <name>Task 2: Remove @google/generative-ai dependency</name>
  <files>package.json</files>
  <action>
Remove @google/generative-ai from package.json dependencies.

Current entry: `"@google/generative-ai": "^0.24.1"`

After removal, run `npm install` to update package-lock.json.

Note: Do NOT remove GOOGLE_API_KEY from .env.local - that's user configuration, not code.
  </action>
  <verify>
Run these commands:
1. `npm run build` - TypeScript compiles successfully
2. `npm test` - All remaining tests pass (gemini-parser.test.ts was deleted)
3. `grep -r "generative-ai" package.json` - Returns no matches
  </verify>
  <done>@google/generative-ai removed from dependencies, npm install completed, build and tests pass</done>
</task>

</tasks>

<verification>
After completing all tasks:
1. `npm run build` passes - No import errors for removed modules
2. `npm test` passes - No test failures (gemini tests removed along with implementation)
3. `ls lib/ai/` shows only: anthropic.ts, claude-parser.ts, schemas/
4. `grep -r "gemini" lib/ app/` returns no matches (case-insensitive)
</verification>

<success_criteria>
- Zero Gemini-related files in lib/ai/
- @google/generative-ai removed from package.json
- TypeScript build succeeds
- All tests pass
- No dangling imports or references to Gemini modules
</success_criteria>

<output>
After completion, create `.planning/phases/22-codebase-cleanup/22-01-SUMMARY.md`
</output>
