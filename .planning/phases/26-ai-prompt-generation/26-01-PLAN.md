---
phase: 26-ai-prompt-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/ai/prompt-generator.ts
  - lib/ai/prompts/intent-analysis.ts
autonomous: true

must_haves:
  truths:
    - "Intent analysis detects uniform mode when user wants same operation on all images"
    - "Intent analysis detects explicit variations when user specifies angles/views"
    - "Intent analysis detects implicit variations when user says 'generate N variations'"
    - "Default to uniform mode when intent is ambiguous"
  artifacts:
    - path: "lib/ai/prompts/intent-analysis.ts"
      provides: "Intent analysis system prompt and types"
      exports: ["IntentAnalysis", "IntentMode", "buildIntentAnalysisPrompt"]
    - path: "lib/ai/prompt-generator.ts"
      provides: "Intent analysis function with extended thinking"
      exports: ["analyzeUserIntent", "PromptGenerationOptions"]
  key_links:
    - from: "lib/ai/prompt-generator.ts"
      to: "lib/ai/anthropic.ts"
      via: "import getAnthropicClient, withRetry, CLAUDE_MODEL"
      pattern: "getAnthropicClient|withRetry|CLAUDE_MODEL"
    - from: "lib/ai/prompt-generator.ts"
      to: "lib/ai/prompts/intent-analysis.ts"
      via: "import buildIntentAnalysisPrompt"
      pattern: "buildIntentAnalysisPrompt"
---

<objective>
Create the intent analysis system that detects whether users want uniform prompts, explicit variations, or implicit variations.

Purpose: Phase 26 requires understanding user intent BEFORE generating prompts. This plan creates the foundation for intelligent prompt generation by analyzing what the user actually wants.

Output: Intent analysis types, system prompt, and analysis function using Claude Sonnet 4.5 with extended thinking.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-ai-prompt-generation/26-RESEARCH.md
@.planning/phases/25-schema-storage/25-01-SUMMARY.md
@lib/ai/anthropic.ts
@lib/ai/prompt-generator.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create intent analysis types and system prompt</name>
  <files>lib/ai/prompts/intent-analysis.ts</files>
  <action>
Create a new file `lib/ai/prompts/intent-analysis.ts` with:

1. **IntentMode type** - Discriminated string union:
   - `'uniform'` - Same prompt for all images (most common)
   - `'explicit-variations'` - User specified what varies (e.g., "front, back, side")
   - `'implicit-variations'` - User wants variations but didn't specify what

2. **IntentAnalysis interface**:
   ```typescript
   interface IntentAnalysis {
     mode: IntentMode;
     confidence: number;  // 0.0 to 1.0
     basePrompt: string;  // Core operation extracted from user prompt
     variations?: string[];  // Only for explicit-variations mode
     reasoning?: string;  // From extended thinking (for debugging)
   }
   ```

3. **buildIntentAnalysisPrompt function** that creates the system prompt for Claude:
   - Takes: `folderOperation: string`, `userFullPrompt: string`, `imageCount: number`
   - Returns: Structured prompt with XML tags following research patterns
   - Include clear examples for each intent mode from RESEARCH.md
   - CRITICAL: Include instruction to default to "uniform" when ambiguous
   - Include confidence threshold guidance (below 0.7 = force uniform)
   - Output format: JSON with mode, confidence, basePrompt, variations

Key instructions to include in system prompt:
- "Default to UNIFORM if ambiguous - most users want same operation on all images"
- "UNIFORM examples: 'put each hijab on a woman's head', 'swap faces to Arab women'"
- "EXPLICIT_VARIATIONS examples: 'front, back, side views', 'angles: 0, 45, 90'"
- "IMPLICIT_VARIATIONS examples: 'generate 5 variations', 'make different versions'"
- "Do NOT add creative embellishments - use ONLY details from user's prompt"

Export: IntentMode, IntentAnalysis, buildIntentAnalysisPrompt
  </action>
  <verify>
File exists at lib/ai/prompts/intent-analysis.ts with:
- IntentMode type exported
- IntentAnalysis interface exported
- buildIntentAnalysisPrompt function exported
- TypeScript compiles without errors: `npx tsc --noEmit lib/ai/prompts/intent-analysis.ts`
  </verify>
  <done>
Intent analysis types and system prompt builder exist and compile without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add intent analysis function to prompt generator</name>
  <files>lib/ai/prompt-generator.ts</files>
  <action>
Enhance `lib/ai/prompt-generator.ts` to add the intent analysis function:

1. **Add imports** at top of file:
   ```typescript
   import { getAnthropicClient, CLAUDE_MODEL, withRetry } from './anthropic';
   import { IntentAnalysis, buildIntentAnalysisPrompt } from './prompts/intent-analysis';
   ```

2. **Add analyzeUserIntent function** (new export):
   ```typescript
   export async function analyzeUserIntent(
     folderOperation: string,
     userFullPrompt: string,
     imageCount: number
   ): Promise<IntentAnalysis>
   ```

   Implementation:
   - Call Claude Sonnet 4.5 with extended thinking (budget_tokens: 3000)
   - Use buildIntentAnalysisPrompt to create the prompt
   - Parse JSON response using pattern from claude-parser.ts (handle markdown code blocks)
   - Extract thinking block for debugging (log first 200 chars)
   - If confidence < 0.7, force mode to 'uniform' with warning log
   - Use withRetry wrapper for resilience

3. **Keep existing generatePerImagePrompt function** unchanged for now (Phase 26 Plan 02 will enhance it)

4. **JSON parsing helper** - Create cleanJsonResponse function:
   - Strip markdown code block markers (```json and ```)
   - Trim whitespace
   - Parse JSON with try/catch
   - Return parsed object or throw clear error

Error handling:
- Wrap entire function in try/catch
- On failure: return default uniform intent with confidence 0.5
- Log errors with context for debugging
  </action>
  <verify>
1. File compiles: `npx tsc --noEmit lib/ai/prompt-generator.ts`
2. Exports check: File exports analyzeUserIntent, generatePerImagePrompt, PromptGenerationOptions
3. Import verification: anthropic.ts exports are imported correctly
  </verify>
  <done>
analyzeUserIntent function exists in prompt-generator.ts, uses extended thinking, handles JSON parsing, and defaults to uniform on low confidence.
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `npx tsc --noEmit`
2. New file exists: `lib/ai/prompts/intent-analysis.ts`
3. Exports available:
   - IntentMode type
   - IntentAnalysis interface
   - buildIntentAnalysisPrompt function
   - analyzeUserIntent function
4. Intent analysis system prompt includes all three modes with examples
5. Low confidence handling defaults to uniform mode
</verification>

<success_criteria>
- Intent analysis types and prompt builder in separate file (lib/ai/prompts/intent-analysis.ts)
- analyzeUserIntent function added to prompt-generator.ts
- Extended thinking used for intent analysis (budget_tokens: 3000)
- Default to uniform mode when confidence < 0.7
- All TypeScript compiles without errors
- Existing generatePerImagePrompt function unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/26-ai-prompt-generation/26-01-SUMMARY.md`
</output>
