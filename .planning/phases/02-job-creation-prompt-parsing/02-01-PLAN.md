---
phase: 02-job-creation-prompt-parsing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/ai/anthropic.ts
  - .env.local
  - .env.example
  - package.json
autonomous: true

must_haves:
  truths:
    - "Anthropic SDK is installed and configured"
    - "Server-side client singleton exists and works"
    - "API key is securely stored in environment variable"
  artifacts:
    - path: "lib/ai/anthropic.ts"
      provides: "Anthropic client singleton"
      exports: ["getAnthropicClient"]
    - path: "package.json"
      contains: "@anthropic-ai/sdk"
  key_links:
    - from: "lib/ai/anthropic.ts"
      to: "process.env.ANTHROPIC_API_KEY"
      via: "environment variable"
      pattern: "process\\.env\\.ANTHROPIC_API_KEY"
---

<objective>
Install and configure the Anthropic SDK for Claude Sonnet 4.5 integration.

Purpose: Foundation for all AI-powered prompt parsing - the SDK enables structured outputs and multi-turn conversations that power the core value of this app.

Output: Server-side Anthropic client singleton ready for use in API routes.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-job-creation-prompt-parsing/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Anthropic SDK and Zod</name>
  <files>package.json</files>
  <action>
    Install the official Anthropic SDK and Zod for schema validation:

    ```bash
    npm install @anthropic-ai/sdk zod
    ```

    Zod is required for structured outputs (type-safe JSON responses from Claude).
  </action>
  <verify>
    - `npm ls @anthropic-ai/sdk` shows package installed
    - `npm ls zod` shows package installed
    - `npx tsc --noEmit` passes
  </verify>
  <done>Both @anthropic-ai/sdk and zod appear in package.json dependencies</done>
</task>

<task type="auto">
  <name>Task 2: Create Anthropic client singleton</name>
  <files>lib/ai/anthropic.ts, .env.local, .env.example</files>
  <action>
    Create server-side Anthropic client with singleton pattern:

    **lib/ai/anthropic.ts:**
    ```typescript
    import Anthropic from '@anthropic-ai/sdk';

    // Singleton pattern for server-side usage
    let client: Anthropic | null = null;

    export function getAnthropicClient(): Anthropic {
      if (!client) {
        const apiKey = process.env.ANTHROPIC_API_KEY;
        if (!apiKey) {
          throw new Error('ANTHROPIC_API_KEY environment variable is not set');
        }
        client = new Anthropic({ apiKey });
      }
      return client;
    }

    // Model constant for consistency
    export const CLAUDE_MODEL = 'claude-sonnet-4-5-20250929';
    ```

    **Add to .env.local:**
    ```
    # Anthropic API
    ANTHROPIC_API_KEY=your-anthropic-api-key-here
    ```

    **Add to .env.example:**
    ```
    # Anthropic API
    ANTHROPIC_API_KEY=sk-ant-...
    ```

    NOTE: Set the actual API key in .env.local (never commit real keys).
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - File exists at lib/ai/anthropic.ts
    - Imports resolve correctly
  </verify>
  <done>
    - getAnthropicClient function exported from lib/ai/anthropic.ts
    - ANTHROPIC_API_KEY in .env.local
    - .env.example updated with placeholder
  </done>
</task>

</tasks>

<verification>
- `npm ls @anthropic-ai/sdk zod` shows both packages
- `npx tsc --noEmit` passes with no errors
- lib/ai/anthropic.ts exports getAnthropicClient and CLAUDE_MODEL
</verification>

<success_criteria>
- Anthropic SDK installed and importable
- Zod installed for schema validation
- Server-side client singleton with proper error handling for missing API key
- Environment variables configured
</success_criteria>

<output>
After completion, create `.planning/phases/02-job-creation-prompt-parsing/02-01-SUMMARY.md`
</output>
