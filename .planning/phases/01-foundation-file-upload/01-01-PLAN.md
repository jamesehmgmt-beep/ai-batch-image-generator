---
phase: 01-foundation-file-upload
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - next.config.ts
  - tailwind.config.ts
  - app/layout.tsx
  - app/globals.css
  - app/page.tsx
  - components/ui/button.tsx
  - components/ui/card.tsx
  - components/ui/input.tsx
  - lib/utils.ts
autonomous: true

must_haves:
  truths:
    - "Next.js dev server starts without errors"
    - "Dark mode UI renders by default"
    - "shadcn/ui components are available and styled"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies"
      contains: "next"
    - path: "app/layout.tsx"
      provides: "Root layout with dark mode"
      contains: "dark"
    - path: "tailwind.config.ts"
      provides: "Tailwind v4 configuration"
      min_lines: 10
  key_links:
    - from: "app/layout.tsx"
      to: "app/globals.css"
      via: "CSS import"
      pattern: "import.*globals.css"
---

<objective>
Create the Next.js 16 project foundation with Tailwind v4, shadcn/ui, and dark mode UI shell.

Purpose: Establish the base application that all other plans build upon. Without this foundation, no other work can proceed.
Output: Working Next.js development server with dark mode UI shell ready for feature implementation.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-file-upload/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js 16 project with Tailwind v4</name>
  <files>
    package.json
    next.config.ts
    tailwind.config.ts
    postcss.config.mjs
    app/globals.css
    app/layout.tsx
    app/page.tsx
    tsconfig.json
  </files>
  <action>
    1. Run `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir=false --import-alias="@/*" --turbopack` to initialize in current directory
       - If directory not empty, create in temp and move files, or use --force if available

    2. Verify Tailwind v4 is installed (check package.json for "tailwindcss": "^4.x")
       - If v3, upgrade: `npm install tailwindcss@latest @tailwindcss/postcss@latest`

    3. Configure dark mode in tailwind.config.ts:
       ```typescript
       import type { Config } from 'tailwindcss'

       const config: Config = {
         darkMode: 'class',
         content: [
           './pages/**/*.{js,ts,jsx,tsx,mdx}',
           './components/**/*.{js,ts,jsx,tsx,mdx}',
           './app/**/*.{js,ts,jsx,tsx,mdx}',
         ],
         theme: {
           extend: {},
         },
         plugins: [],
       }
       export default config
       ```

    4. Update app/globals.css for dark mode defaults:
       ```css
       @import "tailwindcss";

       :root {
         --background: 0 0% 100%;
         --foreground: 240 10% 3.9%;
       }

       .dark {
         --background: 240 10% 3.9%;
         --foreground: 0 0% 98%;
       }

       body {
         background-color: hsl(var(--background));
         color: hsl(var(--foreground));
       }
       ```

    5. Update app/layout.tsx to apply dark class by default:
       ```typescript
       import type { Metadata } from 'next'
       import { Inter } from 'next/font/google'
       import './globals.css'

       const inter = Inter({ subsets: ['latin'] })

       export const metadata: Metadata = {
         title: 'BulkImageGen',
         description: 'AI-powered bulk image generation',
       }

       export default function RootLayout({
         children,
       }: {
         children: React.ReactNode
       }) {
         return (
           <html lang="en" className="dark">
             <body className={inter.className}>{children}</body>
           </html>
         )
       }
       ```
  </action>
  <verify>
    Run `npm run dev` and verify:
    - Server starts on http://localhost:3000
    - Page renders with dark background
    - No console errors
  </verify>
  <done>
    - Next.js 16 dev server runs without errors
    - Dark mode is applied by default (dark background visible)
    - Tailwind CSS classes work (test with any utility class)
  </done>
</task>

<task type="auto">
  <name>Task 2: Initialize shadcn/ui with core components</name>
  <files>
    components.json
    lib/utils.ts
    components/ui/button.tsx
    components/ui/card.tsx
    components/ui/input.tsx
  </files>
  <action>
    1. Initialize shadcn/ui with Tailwind v4 support:
       ```bash
       npx shadcn@latest init
       ```
       When prompted:
       - Style: Default
       - Base color: Zinc (works well with dark mode)
       - CSS variables: Yes
       - Tailwind config location: tailwind.config.ts
       - Components location: @/components
       - Utils location: @/lib/utils
       - React Server Components: Yes

    2. Install core UI components needed for this phase:
       ```bash
       npx shadcn@latest add button card input progress
       ```

    3. Verify lib/utils.ts exists with cn() helper:
       ```typescript
       import { type ClassValue, clsx } from "clsx"
       import { twMerge } from "tailwind-merge"

       export function cn(...inputs: ClassValue[]) {
         return twMerge(clsx(inputs))
       }
       ```

    4. Create a simple test page at app/page.tsx to verify components work:
       ```typescript
       import { Button } from "@/components/ui/button"
       import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card"

       export default function Home() {
         return (
           <main className="min-h-screen p-8">
             <Card className="max-w-md mx-auto">
               <CardHeader>
                 <CardTitle>BulkImageGen</CardTitle>
                 <CardDescription>AI-powered bulk image generation</CardDescription>
               </CardHeader>
               <CardContent>
                 <Button className="w-full">Get Started</Button>
               </CardContent>
             </Card>
           </main>
         )
       }
       ```
  </action>
  <verify>
    Run `npm run dev` and verify:
    - Card component renders with proper dark mode styling
    - Button shows hover states
    - No TypeScript errors in components
  </verify>
  <done>
    - shadcn/ui initialized with components.json
    - Button, Card, Input, Progress components installed
    - Components render correctly in dark mode
    - cn() utility available from @/lib/utils
  </done>
</task>

</tasks>

<verification>
Run these checks to verify the plan is complete:

1. `npm run dev` - Server starts without errors
2. Visit http://localhost:3000 - Dark mode UI renders
3. `npm run build` - Build completes without errors
4. Check components/ui/ directory has button.tsx, card.tsx, input.tsx
</verification>

<success_criteria>
- Next.js 16 development server runs on localhost:3000
- Page displays with dark background (zinc/slate dark theme)
- shadcn/ui Card and Button render with proper styling
- No TypeScript or ESLint errors
- Build completes successfully
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-file-upload/01-01-SUMMARY.md`
</output>
