---
phase: 22-codebase-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/ai/gemini-parser.test.ts (deleted in 22-01, skip if not present)
autonomous: true

must_haves:
  truths:
    - "All test files test code that exists"
    - "No orphaned test files remain"
    - "Test suite runs successfully"
  artifacts:
    - path: "lib/job/generation-count.test.ts"
      provides: "Unit tests for calculateGenerationCount"
    - path: "lib/queue/__tests__/retry-strategies.test.ts"
      provides: "Unit tests for retry strategies"
    - path: "lib/db/__tests__/job-queries.test.ts"
      provides: "Unit tests for job queries"
  key_links:
    - from: "lib/job/generation-count.test.ts"
      to: "lib/job/generation-count.ts"
      via: "import calculateGenerationCount"
      pattern: "calculateGenerationCount"
---

<objective>
Verify test file integrity after Gemini removal

Purpose: After removing gemini-parser.test.ts in Plan 22-01, verify that all remaining test files test existing code. This prevents orphaned tests that would fail or test non-existent functionality.

Output: Verified test suite with all tests testing real code
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-codebase-cleanup/22-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit remaining test files</name>
  <files>
    lib/job/generation-count.test.ts
    lib/queue/__tests__/retry-strategies.test.ts
    lib/db/__tests__/job-queries.test.ts
  </files>
  <action>
Verify each remaining test file tests code that exists:

1. lib/job/generation-count.test.ts
   - Should import from lib/job/generation-count.ts
   - Verify source file exists

2. lib/queue/__tests__/retry-strategies.test.ts
   - Should test retry strategies (verify matching source exists)

3. lib/db/__tests__/job-queries.test.ts
   - Should test job query functions (verify matching source exists)

For each test file:
- Check the import statements reference existing files
- Verify the tested functions/modules exist
- Run the individual test file to confirm it passes

If any test imports from deleted modules (like gemini), update or remove it.
  </action>
  <verify>
Run `npm test` - All tests should pass.
Count of test files: Should be 3 (after gemini-parser.test.ts removal in 22-01)
  </verify>
  <done>All 3 remaining test files verified to test existing code, npm test passes</done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite verification</name>
  <files>None (verification only)</files>
  <action>
Run the complete test suite to verify all tests pass after cleanup:

1. Run `npm test` to execute all tests
2. Verify no tests are skipped due to missing dependencies
3. Verify no tests fail due to missing imports
4. Check test output for any warnings about missing modules

Expected test files after cleanup:
- lib/job/generation-count.test.ts (10 tests for calculateGenerationCount)
- lib/queue/__tests__/retry-strategies.test.ts
- lib/db/__tests__/job-queries.test.ts

If gemini-parser.test.ts still exists (22-01 not yet run), note it will be removed.
  </action>
  <verify>
`npm test` output shows:
- All test suites pass
- No "Cannot find module" errors
- No skipped tests due to missing dependencies
  </verify>
  <done>Full test suite passes with no orphaned tests or missing module errors</done>
</task>

</tasks>

<verification>
After completing all tasks:
1. `npm test` passes with all test files
2. No test file imports from deleted modules (gemini.ts, gemini-parser.ts)
3. Each test file's source file exists and is imported correctly
</verification>

<success_criteria>
- All remaining test files (3) test existing code
- `npm test` passes with zero failures
- No orphaned test files exist
- No warnings about missing modules in test output
</success_criteria>

<output>
After completion, create `.planning/phases/22-codebase-cleanup/22-02-SUMMARY.md`
</output>
