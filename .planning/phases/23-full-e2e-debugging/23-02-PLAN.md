---
phase: 23-full-e2e-debugging
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "User can enter natural language prompt"
    - "Claude API returns valid ParsedJob structure"
    - "Folder operations match user prompt intent"
    - "Error messages are clear for invalid prompts"
    - "Navigation to review page works with parsed data"
  artifacts:
    - path: "app/api/ai/parse/route.ts"
      provides: "Claude API integration"
    - path: "lib/ai/claude-parser.ts"
      provides: "Claude parsing logic"
    - path: "app/(protected)/job/create-job/page.tsx"
      provides: "Prompt input UI"
  key_links:
    - from: "app/(protected)/job/create-job/page.tsx"
      to: "/api/ai/parse"
      via: "fetch POST request"
      pattern: "fetch.*api/ai/parse"
    - from: "lib/ai/claude-parser.ts"
      to: "Anthropic API"
      via: "@anthropic-ai/sdk client"
      pattern: "anthropic\\.messages\\.create"
---

<objective>
Test and fix the AI parsing flow (DEBG-02)

Purpose: Verify that Claude AI correctly parses user prompts into structured job definitions. This is the critical bridge between natural language input and executable job structure.

Output: Verified AI parsing flow with any bugs fixed, documented test results.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-full-e2e-debugging/23-RESEARCH.md
@.planning/phases/23-full-e2e-debugging/23-01-SUMMARY.md

Key files to understand:
@app/api/ai/parse/route.ts
@lib/ai/claude-parser.ts
@lib/ai/schemas/job.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Claude API configuration</name>
  <files>None (verification only)</files>
  <action>
Verify Claude API is properly configured:

1. Check that ANTHROPIC_API_KEY environment variable is set
2. Verify claude-parser.ts uses correct model ID (claude-sonnet-4-5-20250514)
3. Review the system prompt for job parsing
4. Ensure production server from 23-01 is still running

Run a quick API health check:
- The parse route should be accessible
- Rate limiting error messages should be meaningful
  </action>
  <verify>
- ANTHROPIC_API_KEY is set in environment
- Model ID is valid and current
- Server is running at http://localhost:3000
  </verify>
  <done>Claude API configuration verified</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual AI parsing testing</name>
  <what-built>AI parsing flow using Claude Sonnet 4.5 with extended thinking</what-built>
  <how-to-verify>
Prerequisites: Complete upload from 23-01 first (have folders uploaded in session).

Navigate to http://localhost:3000/create-job and test:

**Test 1: Simple global prompt**
1. Enter prompt: "Make all images look like watercolor paintings"
2. Click "Parse with AI" (or equivalent button)
3. Verify:
   - [ ] Loading state shows while parsing
   - [ ] Response returns within 60 seconds
   - [ ] ParsedJob shows in console/UI
   - [ ] understood: true in response
   - [ ] Each folder has operation matching prompt

**Test 2: Per-folder specific prompts**
1. Upload folders "Landscapes" and "Portraits" (or use existing folders)
2. Enter prompt: "For Landscapes, add dramatic sunset colors. For Portraits, convert to black and white."
3. Verify:
   - [ ] Each folder gets its specific operation
   - [ ] Folder paths match uploaded folders (case-insensitive)
   - [ ] No folders are missing from parsed result

**Test 3: Complex prompt with exclusions**
1. Enter prompt: "Apply vintage filter to all images except thumbnail.jpg and preview.png"
2. Verify:
   - [ ] excludedFiles array contains specified files
   - [ ] Operation applies to remaining files

**Test 4: Model selection in prompt**
1. Enter prompt: "Use Seedream model to create anime style versions"
2. Verify:
   - [ ] model field shows "seedream" in folder operations
   - [ ] Seedream-specific fields (quality, imageSize) have defaults

**Test 5: Error handling**
1. Enter empty prompt and try to parse
2. Verify:
   - [ ] Clear error message shown
   - [ ] No crash or console errors

3. Enter gibberish prompt: "asdfhjkl234567890"
4. Verify:
   - [ ] AI attempts to interpret or returns understood: false
   - [ ] User gets meaningful feedback

**Test 6: Navigation to review**
1. After successful parse, click continue/next
2. Verify:
   - [ ] Navigation to /job/review works
   - [ ] ParsedJob data is displayed on review page
   - [ ] No "missing data" errors

Record in browser DevTools Network tab:
- Request payload to /api/ai/parse
- Response structure and timing
- Any 4xx/5xx errors

Document any issues found with reproduction steps.
  </how-to-verify>
  <resume-signal>
Type "approved" if all tests pass.
Type "issues: [description]" if problems found.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Fix any bugs discovered</name>
  <files>Determined by issues found</files>
  <action>
If issues were reported in Task 2, fix them:

Common AI parsing issues and fixes:

1. **Timeout/slow response**: Increase timeout in fetch call, verify extended thinking budget
2. **Wrong folder paths**: Check case-insensitive matching in claude-parser.ts
3. **Missing model field**: Update system prompt to always include model
4. **excludedFiles not working**: Verify schema allows excludedFiles array
5. **Rate limiting**: Add proper retry logic with exponential backoff
6. **understood: false for valid prompts**: Review system prompt clarity
7. **Navigation fails**: Check setParsedJob is called before navigation

For each bug:
1. Identify root cause in API route or parser
2. Implement fix
3. Add error handling improvements if needed
4. Test the fix locally

If NO issues found, skip this task (log "No issues found").
  </action>
  <verify>
- All reported issues are fixed
- AI parsing returns valid ParsedJob for test prompts
- Error handling is improved
- npm run build still passes
  </verify>
  <done>All AI parsing bugs fixed and verified</done>
</task>

</tasks>

<verification>
- Claude API responds correctly to prompts
- All 6 manual test scenarios pass
- Any discovered bugs are fixed
- AI parsing flow is fully functional
</verification>

<success_criteria>
- User can enter natural language prompts
- Claude AI returns valid, accurate ParsedJob structures
- Folder operations correctly match user intent
- Error handling provides clear user feedback
- Session state includes parsed job for next step
</success_criteria>

<output>
After completion, create `.planning/phases/23-full-e2e-debugging/23-02-SUMMARY.md`
</output>
