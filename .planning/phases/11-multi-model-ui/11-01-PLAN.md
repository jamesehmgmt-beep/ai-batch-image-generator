---
phase: 11-multi-model-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/job/model-selector.tsx
  - components/job/model-settings.tsx
autonomous: true

must_haves:
  truths:
    - "User sees model selector with Nano Banana Pro and Seedream 4.5 Edit options"
    - "Selecting a model updates the parsedJob.job.model field in JobContext"
    - "Model-specific fields render conditionally (resolution for Nano, quality/imageSize for Seedream)"
  artifacts:
    - path: "components/job/model-selector.tsx"
      provides: "Model selection dropdown component"
      exports: ["ModelSelector"]
      min_lines: 40
    - path: "components/job/model-settings.tsx"
      provides: "Conditional model-specific settings fields"
      exports: ["ModelSettings"]
      min_lines: 60
  key_links:
    - from: "components/job/model-selector.tsx"
      to: "lib/session/job-context.tsx"
      via: "useJobContext hook"
      pattern: "useJobContext"
    - from: "components/job/model-selector.tsx"
      to: "lib/models/types.ts"
      via: "import capabilities"
      pattern: "NANO_BANANA_CAPABILITIES|SEEDREAM_CAPABILITIES"
---

<objective>
Create model selector and model-specific settings components for multi-model UI.

Purpose: Enable users to choose between Nano Banana Pro and Seedream 4.5 Edit models, with dynamic form fields that change based on the selected model.
Output: Two reusable components (ModelSelector, ModelSettings) that integrate with JobContext for state management.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-multi-model-ui/11-RESEARCH.md

# Source types and capabilities
@lib/models/types.ts
@lib/ai/schemas/job.ts
@lib/session/job-context.tsx

# Existing component patterns
@components/job/format-selector.tsx
@components/job/mode-override.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ModelSelector component</name>
  <files>components/job/model-selector.tsx</files>
  <action>
Create a new component `components/job/model-selector.tsx` with:

1. Import shadcn/ui Select, useJobContext, and model capabilities from lib/models/types.ts
2. Define ModelSelectorProps interface (optional value/onChange props for controlled mode, or use JobContext directly)
3. Implement ModelSelector component:
   - Use Select component from shadcn/ui (already installed)
   - Map NANO_BANANA_CAPABILITIES and SEEDREAM_CAPABILITIES to Select options:
     ```tsx
     const models = [NANO_BANANA_CAPABILITIES, SEEDREAM_CAPABILITIES];
     // In SelectContent:
     {models.map((cap) => (
       <SelectItem key={cap.id} value={cap.id}>
         <span className="font-medium">{cap.displayName}</span>
         <span className="text-muted-foreground text-sm ml-2">
           {cap.id === 'nano-banana-pro'
             ? `3 resolutions, ${cap.supportedAspectRatios.length} aspect ratios`
             : `${Object.keys(cap.costPerGeneration).length} quality tiers, ${cap.supportedAspectRatios.length} sizes`}
         </span>
       </SelectItem>
     ))}
     ```
   - On selection change, call updateParsedJob to:
     a. Set job.model to new model
     b. Update ALL folders to new model with correct default fields:
        - Nano: { model: 'nano-banana-pro', resolution: '2K', quality: undefined, imageSize: undefined }
        - Seedream: { model: 'seedream-4.5-edit', quality: 'basic', imageSize: 'landscape_16_9', resolution: undefined }

IMPORTANT: When switching models, MUST explicitly set unused fields to undefined to satisfy Zod discriminated union validation. Do NOT just spread existing folder - clear old model's fields.

Follow format-selector.tsx pattern for styling consistency (Card wrapper, label above selector).
  </action>
  <verify>
1. File exists: `ls components/job/model-selector.tsx`
2. TypeScript compiles: `npx tsc --noEmit`
3. Component exports ModelSelector: grep for "export function ModelSelector"
4. Uses updateParsedJob: grep for "updateParsedJob"
5. Maps capabilities to options: grep for "NANO_BANANA_CAPABILITIES" and "models.map"
  </verify>
  <done>
ModelSelector component created with Select dropdown showing both models with displayName and description from capabilities, updating job.model and all folder.model fields on change with proper field clearing for discriminated union.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ModelSettings component</name>
  <files>components/job/model-settings.tsx</files>
  <action>
Create a new component `components/job/model-settings.tsx` with:

1. Import shadcn/ui Select, Label, useJobContext, and model capabilities
2. Define ModelSettingsProps interface (folderIndex?: number for per-folder, or omit for global)
3. Implement ModelSettings component:
   - Read parsedJob.job.model to determine which fields to show
   - If model === 'nano-banana-pro':
     - Render Resolution selector (1K, 2K, 4K) with price per tier
     - Use NANO_BANANA_CAPABILITIES.costPerGeneration for pricing
     - Render Aspect Ratio selector using NANO_BANANA_CAPABILITIES.supportedAspectRatios (format: '16:9', '1:1', etc.)
   - If model === 'seedream-4.5-edit':
     - Render Quality selector (Basic, High)
     - Render Image Size selector using SEEDREAM_CAPABILITIES.supportedAspectRatios (format: 'landscape_16_9', 'square', etc.)
     - NOTE: Seedream uses imageSize field which doubles as aspect ratio - no separate aspectRatio field needed

4. CRITICAL - Aspect Ratio Format Handling:
   - Nano uses colon-separated format: '16:9', '1:1', '3:2'
   - Seedream uses underscore preset format: 'landscape_16_9', 'square', 'portrait_4_3'
   - These are DIFFERENT fields - Nano uses aspectRatio, Seedream uses imageSize
   - Display names for Seedream: map 'landscape_16_9' -> 'Landscape 16:9', 'square' -> 'Square', etc.
   ```tsx
   function formatSeedreamSize(size: string): string {
     return size.split('_').map(s => s.charAt(0).toUpperCase() + s.slice(1)).join(' ');
   }
   ```

5. Helper function updateAllFolders(field, value) to update a field across ALL folders in parsedJob.job.folders (for global settings mode)

PITFALL AVOIDANCE:
- Check model with if/else BEFORE accessing model-specific fields
- Use type narrowing: `if (folder.model === 'nano-banana-pro') { folder.resolution }`
- Default to first folder for display, but update ALL folders on change
- Do NOT try to convert between Nano aspectRatio and Seedream imageSize - they are independent

Style: Use grid layout (2 columns on lg) for settings, consistent with ModeOverride component.
  </action>
  <verify>
1. File exists: `ls components/job/model-settings.tsx`
2. TypeScript compiles: `npx tsc --noEmit`
3. Component exports ModelSettings: grep for "export function ModelSettings"
4. Conditional rendering present: grep for "nano-banana-pro" and "seedream-4.5-edit"
5. Updates all folders: grep for "folders.map" or similar array update pattern
6. Handles both aspect ratio formats: grep for "aspectRatio" and "imageSize"
  </verify>
  <done>
ModelSettings component created with conditional rendering of Resolution+AspectRatio (Nano) or Quality+ImageSize (Seedream) fields, with proper format handling for each model's aspect ratio system, updating all folders when changed.
  </done>
</task>

</tasks>

<verification>
1. Both components compile without TypeScript errors: `npx tsc --noEmit`
2. Components are self-contained and use JobContext for state
3. ModelSelector properly clears old model fields on switch
4. ModelSettings shows correct fields based on model discriminator
5. Aspect ratio/imageSize format differences handled correctly per model
</verification>

<success_criteria>
- ModelSelector.tsx exports ModelSelector component with Select dropdown
- ModelSelector maps NANO_BANANA_CAPABILITIES and SEEDREAM_CAPABILITIES to display options with displayName and description
- ModelSettings.tsx exports ModelSettings component with conditional model fields
- Both components use JobContext for state management
- Model switch properly clears unused fields (resolution undefined for Seedream, quality/imageSize undefined for Nano)
- Nano shows aspectRatio selector with colon format ('16:9')
- Seedream shows imageSize selector with underscore format ('landscape_16_9') with human-readable labels
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/11-multi-model-ui/11-01-SUMMARY.md`
</output>
