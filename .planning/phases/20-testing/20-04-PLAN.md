---
phase: 20-testing
plan: 04
type: execute
wave: 3
depends_on: ["20-01", "20-02", "20-03"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "All automated tests pass (npm run test)"
    - "E2E tests execute without configuration errors (npm run test:e2e)"
    - "Manual workflow test completes successfully: upload -> parse -> confirm -> execute -> download"
  artifacts: []
  key_links: []
---

<objective>
Final verification checkpoint for Phase 20 Testing - run complete test suite and perform manual UAT.

Purpose: Ensure all testing requirements (TEST-01, TEST-02, TEST-03) are satisfied and the application works end-to-end.
Output: Confirmed test coverage and working application ready for v3.0 milestone completion.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-testing/20-01-SUMMARY.md
@.planning/phases/20-testing/20-02-SUMMARY.md
@.planning/phases/20-testing/20-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run Complete Test Suite</name>
  <files></files>
  <action>
Execute all automated tests and capture results:

1. Run unit and integration tests:
```bash
npm run test
```

2. Run E2E tests:
```bash
npm run test:e2e
```

3. Verify all tests pass.

If any tests fail, diagnose and fix the issues before proceeding to manual verification.
  </action>
  <verify>
All tests pass with no failures or skipped tests (except intentionally skipped).
  </verify>
  <done>Automated test suite executes successfully with all tests passing.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete test infrastructure for v3.0:
- Unit tests for calculateGenerationCount (TEST-01)
- Integration tests for Gemini parser with mocked SDK (TEST-02)
- E2E test foundation with Playwright (TEST-03)
  </what-built>
  <how-to-verify>
**Automated Tests:**
1. Run `npm run test` and verify all unit/integration tests pass
2. Run `npm run test:e2e` and verify E2E tests execute

**Manual End-to-End Workflow (Final UAT):**
1. Start the dev server: `npm run dev`
2. Open http://localhost:3000
3. Upload 2-3 folders of test images
4. Enter a prompt: "swap faces in folder 1 to professional headshots"
5. Verify AI parsing returns valid interpretation
6. Click through to confirmation page
7. Verify generation count matches expected
8. Proceed to cost estimation
9. Execute the job
10. Monitor progress on preview page
11. Download results when complete
12. Verify downloaded zip contains generated images

**Checklist:**
- [ ] Unit tests pass (calculateGenerationCount)
- [ ] Integration tests pass (Gemini parser)
- [ ] E2E tests run without errors
- [ ] Manual workflow: upload works
- [ ] Manual workflow: AI parsing works
- [ ] Manual workflow: confirmation shows correct counts
- [ ] Manual workflow: execution completes
- [ ] Manual workflow: download works
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
- [ ] `npm run test` passes with all unit/integration tests
- [ ] `npm run test:e2e` executes without configuration errors
- [ ] Manual end-to-end workflow completes successfully
- [ ] All TEST-01, TEST-02, TEST-03 requirements verified
</verification>

<success_criteria>
- All automated tests pass
- Manual UAT workflow completes: upload -> parse -> confirm -> execute -> download
- v3.0 milestone ready for completion
</success_criteria>

<output>
After completion, create `.planning/phases/20-testing/20-04-SUMMARY.md`
</output>
