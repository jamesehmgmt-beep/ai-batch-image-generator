---
phase: 15-interpretation-confirmation-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/job/interpretation-summary.tsx
  - components/job/folder-exclusions.tsx
autonomous: true

must_haves:
  truths:
    - "Summary shows total generation count for all folders"
    - "Summary shows breakdown by model (Nano Banana vs Seedream)"
    - "Exclusions display shows files that will be skipped"
  artifacts:
    - path: "components/job/interpretation-summary.tsx"
      provides: "Generation count summary with model breakdown"
      min_lines: 50
      exports: ["InterpretationSummary"]
    - path: "components/job/folder-exclusions.tsx"
      provides: "Excluded files display component"
      min_lines: 30
      exports: ["FolderExclusions"]
  key_links:
    - from: "components/job/interpretation-summary.tsx"
      to: "lib/job/generation-count.ts"
      via: "calculateGenerationCount import"
      pattern: "import.*calculateGenerationCount.*from.*lib/job"
---

<objective>
Create two display components for the interpretation confirmation UI: InterpretationSummary and FolderExclusions.

Purpose: These components display the AI's interpretation of the user's request, showing total generation counts with model breakdown and excluded files. Users need this information to verify the AI understood correctly before proceeding.

Output: Two React components ready for integration into the confirm page.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-interpretation-confirmation-ui/15-RESEARCH.md

# Key reference files
@lib/job/generation-count.ts
@lib/types/job.ts
@components/job/parsed-job-review.tsx
@lib/session/job-context.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create InterpretationSummary component</name>
  <files>components/job/interpretation-summary.tsx</files>
  <action>
Create a client component that displays:
1. Total generation count across all folders (use calculateGenerationCount from lib/job/generation-count.ts)
2. Breakdown by model (group folders by model, sum counts per model)
3. Breakdown by folder (folder path + count)

Props interface:
```typescript
interface InterpretationSummaryProps {
  job: NonNullable<ParsedJob['job']>;
  fileCountByFolder: Record<string, number>;
}
```

Implementation:
- Import calculateGenerationCount from '@/lib/job/generation-count'
- Calculate total: sum of calculateGenerationCount(folder, fileCountByFolder[folder.folderPath]) for each folder
- Group by model: reduce folders into { 'nano-banana-pro': N, 'seedream-4.5-edit': M } structure
- Display in a Card with CardHeader "AI Interpretation Summary"
- Show large total count prominently (text-2xl font-bold)
- Show model breakdown with Badge for each model type
- Show per-folder breakdown as a list with folder path and count

Use existing UI patterns from parsed-job-review.tsx:
- Card, CardHeader, CardTitle, CardContent from @/components/ui/card
- Badge from @/components/ui/badge
- Folder icon from lucide-react

Do NOT:
- Fetch data or call APIs - this is a pure display component
- Handle edit mode - that's for plan 02
  </action>
  <verify>
Run `npx tsc --noEmit` - no type errors
Component file exists with correct exports
  </verify>
  <done>
InterpretationSummary component displays total count, model breakdown, and folder breakdown using calculateGenerationCount
  </done>
</task>

<task type="auto">
  <name>Task 2: Create FolderExclusions component</name>
  <files>components/job/folder-exclusions.tsx</files>
  <action>
Create a client component that displays excluded files for a folder.

Props interface:
```typescript
interface FolderExclusionsProps {
  folderPath: string;
  excludedFiles: string[];
  isEditable?: boolean;
  onRemove?: (fileName: string) => void;
  onAdd?: (fileName: string) => void;
}
```

Display mode (isEditable=false):
- Show list of excluded files as destructive Badges (matches existing pattern in parsed-job-review.tsx)
- Show FileX icon with count header

Edit mode (isEditable=true):
- Same display as view mode
- Each Badge has an X button to remove (calls onRemove)
- Input field + Add button to add new exclusion (calls onAdd)
- Input clears after adding

Use existing patterns:
- Badge with variant="destructive" for excluded files
- FileX icon from lucide-react
- X icon for remove button
- Input and Button from @/components/ui

Handle empty state:
- If excludedFiles is empty and not editable, return null (nothing to show)
- If excludedFiles is empty and editable, show just the add input

Do NOT:
- Manage state internally - parent controls via props
- Show this component for folders with imageOperations (mutual exclusivity - parent decides)
  </action>
  <verify>
Run `npx tsc --noEmit` - no type errors
Component file exists with correct exports
  </verify>
  <done>
FolderExclusions component displays excluded files in view mode with badges, and allows add/remove in edit mode
  </done>
</task>

</tasks>

<verification>
Both components compile without errors:
```bash
npx tsc --noEmit
```

Components export correctly:
```bash
grep -l "export.*InterpretationSummary" components/job/interpretation-summary.tsx
grep -l "export.*FolderExclusions" components/job/folder-exclusions.tsx
```
</verification>

<success_criteria>
- [ ] InterpretationSummary shows total generation count using calculateGenerationCount
- [ ] InterpretationSummary shows model breakdown (badges for each model with count)
- [ ] InterpretationSummary shows per-folder breakdown (folder paths with counts)
- [ ] FolderExclusions displays excluded files as destructive badges
- [ ] FolderExclusions supports edit mode with add/remove functionality
- [ ] Both components compile without TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/15-interpretation-confirmation-ui/15-01-SUMMARY.md`
</output>
