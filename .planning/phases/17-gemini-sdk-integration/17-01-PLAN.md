# Plan 17-01: Gemini SDK Integration [COMPLETE]

## Context

Phase 17 sets up the Google AI SDK for Gemini models. This is infrastructure-only - no user-facing changes yet.

## Requirements Addressed

- **GEMI-04**: Gemini SDK integrated with proper error handling and retry logic
- **GEMI-05**: API key configuration via environment variable (GOOGLE_AI_API_KEY)

## Tasks

### Task 1: Install Google AI SDK

```bash
npm install @google/generative-ai
```

### Task 2: Create Gemini Client Module

**File**: `lib/ai/gemini.ts`

- Singleton pattern matching existing `anthropic.ts`
- Model constants for Gemini Flash and Pro
- `getGeminiClient()` and `getGeminiModel()` helpers
- `withRetry()` wrapper with exponential backoff
- Retry on 429, 5xx, network errors

### Task 3: Configure Environment Variable

**File**: `.env.local`

Added `GOOGLE_AI_API_KEY` with provided API key.

## Verification

- TypeScript compiles without errors
- SDK exports are accessible from other modules

## Manual Test Checkpoint

NO - SDK setup only, no user-facing changes to test
