---
phase: 11-multi-model-ui
plan: 03
type: execute
wave: 2
depends_on: ["11-01", "11-02"]
files_modified:
  - components/job/cost-estimate.tsx
  - app/(protected)/job/cost/page.tsx
autonomous: false

must_haves:
  truths:
    - "Cost page shows model selector allowing user to switch models"
    - "Cost page shows model-specific settings (resolution or quality)"
    - "Cost page shows prompt mode toggle with per-folder inputs when active"
    - "Cost breakdown displays by-model section when multiple models used"
    - "Changing model or settings updates cost breakdown in real-time"
  artifacts:
    - path: "components/job/cost-estimate.tsx"
      provides: "Updated cost breakdown with by-model section"
      exports: ["CostEstimate"]
      contains: "byModel"
    - path: "app/(protected)/job/cost/page.tsx"
      provides: "Complete multi-model UI integration"
      contains: "ModelSelector"
  key_links:
    - from: "app/(protected)/job/cost/page.tsx"
      to: "components/job/model-selector.tsx"
      via: "import and render"
      pattern: "import.*ModelSelector"
    - from: "app/(protected)/job/cost/page.tsx"
      to: "components/job/prompt-mode-selector.tsx"
      via: "import and render"
      pattern: "import.*PromptModeSelector"
    - from: "components/job/cost-estimate.tsx"
      to: "lib/job/cost-estimation.ts"
      via: "byModel from CostBreakdown"
      pattern: "byModel\\.map"
---

<objective>
Integrate multi-model UI components into cost page and enhance cost breakdown display.

Purpose: Wire ModelSelector, ModelSettings, and PromptModeSelector into the cost page, update CostEstimate to show model-specific breakdown, and verify the complete user flow.
Output: Updated cost page with full multi-model support and enhanced cost visualization.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-multi-model-ui/11-RESEARCH.md

# Components from prior plans (read SUMMARYs after execution)
@.planning/phases/11-multi-model-ui/11-01-SUMMARY.md
@.planning/phases/11-multi-model-ui/11-02-SUMMARY.md

# Files to modify
@components/job/cost-estimate.tsx
@app/(protected)/job/cost/page.tsx

# Supporting modules
@lib/job/cost-estimation.ts
@lib/models/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CostEstimate with model breakdown</name>
  <files>components/job/cost-estimate.tsx</files>
  <action>
Update `components/job/cost-estimate.tsx` to display model-specific cost breakdown:

1. Add new section "By Model" between "Estimated Cost" and "By Resolution":
   - Only render if breakdown.byModel.length > 1 (skip for single-model jobs)
   - Map over byModel array:
     a. Display model name (use displayName lookup or mapping)
     b. Display imageCount with "images" suffix
     c. Display average costPerImage
     d. Display totalCost for that model
   - Style similar to byFolder section (flex between, font-medium names)

2. Add model indicator to byFolder items:
   - Show small Badge with model name next to tier badge
   - Only show if multiple models in job (check byModel.length > 1)

3. Update pricing note at bottom:
   - Change from "kie.ai Nano Banana Pro pricing" to "kie.ai pricing varies by model"
   - List both: "Nano Banana Pro: $0.134-$0.24/image, Seedream 4.5 Edit: $0.032/image"

4. Import Layers icon from lucide-react for "By Model" section header

Helper function:
```typescript
function getModelDisplayName(modelId: string): string {
  return modelId === 'nano-banana-pro' ? 'Nano Banana Pro' : 'Seedream 4.5 Edit';
}
```

Keep existing byResolution section for backward compatibility (useful for Nano-only jobs).
  </action>
  <verify>
1. TypeScript compiles: `npx tsc --noEmit`
2. byModel section exists: grep for "By Model" in cost-estimate.tsx
3. Conditional rendering: grep for "byModel.length > 1"
4. Updated pricing note: grep for "varies by model"
  </verify>
  <done>
CostEstimate component updated with conditional "By Model" section, model badges in folder breakdown, and updated pricing note for multi-model support.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate components into Cost page</name>
  <files>app/(protected)/job/cost/page.tsx</files>
  <action>
Update `app/(protected)/job/cost/page.tsx` to integrate multi-model UI components:

1. Add imports:
   - ModelSelector from @/components/job/model-selector
   - ModelSettings from @/components/job/model-settings
   - PromptModeSelector from @/components/job/prompt-mode-selector
   - PerFolderPrompts from @/components/job/per-folder-prompts

2. Insert new sections BEFORE CostEstimate component (order matters for UX):

   a. Model Selection Card:
      - Card with CardHeader "Generation Model"
      - CardDescription: "Choose which AI model to use for image generation"
      - Render <ModelSelector /> inside CardContent

   b. Model Settings Card:
      - Card with CardHeader "Model Settings"
      - CardDescription: Dynamic based on model (e.g., "Configure Nano Banana Pro parameters")
      - Render <ModelSettings /> inside CardContent

   c. Prompt Configuration Card:
      - Card with CardHeader "Prompt Configuration"
      - CardDescription: "Choose global prompt or per-folder prompts"
      - Render <PromptModeSelector /> inside CardContent
      - PromptModeSelector should contain PerFolderPrompts in its TabsContent

3. Verify cost recalculation:
   - costBreakdown useMemo already depends on [parsedJob?.job, fileCountByFolder]
   - When model/settings change via components, parsedJob.job changes, triggering recalc
   - No additional wiring needed if useMemo dependencies are correct

4. Update max reference photos based on model:
   - Get maxPhotos from model capabilities instead of hardcoded 7
   - Import NANO_BANANA_CAPABILITIES, SEEDREAM_CAPABILITIES
   - Compute: `const maxPhotos = (parsedJob?.job?.model === 'seedream-4.5-edit' ? 14 : 8);`
   - Pass to ReferencePhotos component

LAYOUT ORDER (top to bottom):
1. Header (existing)
2. Model Selection (NEW)
3. Model Settings (NEW)
4. Prompt Configuration (NEW)
5. Cost breakdown (existing)
6. Mode override (existing)
7. Reference photos (existing - with dynamic max)
8. Format selector (existing)
9. Warning + Error + Actions (existing)
  </action>
  <verify>
1. TypeScript compiles: `npx tsc --noEmit`
2. All imports present: grep for "ModelSelector" and "PromptModeSelector" in cost/page.tsx
3. Cards rendered: grep for "Generation Model" and "Prompt Configuration"
4. Dynamic maxPhotos: grep for "maxPhotos" with model conditional
  </verify>
  <done>
Cost page updated with Model Selection, Model Settings, and Prompt Configuration cards before cost breakdown, reference photo limit respects model capabilities.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete multi-model UI</name>
  <files>None (verification only)</files>
  <what-built>Complete multi-model UI on cost page including model selector, model-specific settings, prompt mode toggle, per-folder prompts, and enhanced cost breakdown</what-built>
  <how-to-verify>
1. Start dev server: `npm run dev`
2. Navigate to http://localhost:3000/create-job
3. Upload test images (multiple folders if possible)
4. Enter a prompt and let AI parse it
5. Click "Confirm & View Cost" to go to cost page
6. Verify on cost page:
   - [ ] Model selector shows "Nano Banana Pro" and "Seedream 4.5 Edit"
   - [ ] Switching model updates the settings section (Resolution vs Quality/ImageSize)
   - [ ] Cost breakdown updates when model changes
   - [ ] Prompt mode toggle shows "Global Prompt" and "Per-Folder Prompts" tabs
   - [ ] Selecting Per-Folder shows one textarea per uploaded folder
   - [ ] By Model section appears in cost breakdown (may need to use both models in job)
   - [ ] Reference photos section shows correct max (8 for Nano, 14 for Seedream)
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. All components compile: `npx tsc --noEmit`
2. Cost page renders without errors
3. Model switching updates all folders and recalculates cost
4. Prompt mode switching preserves data appropriately
5. By Model section only shows for multi-model jobs
</verification>

<success_criteria>
- Cost page displays Model Selection, Model Settings, Prompt Configuration cards
- Model selector changes model and updates all folder configs
- Model settings show correct fields based on selected model
- Prompt mode toggle switches between global and per-folder
- Per-folder mode shows one input per uploaded folder
- Cost breakdown shows By Model section when multiple models used
- Reference photo limit is model-aware (8 vs 14)
- Human verification confirms visual and functional correctness
</success_criteria>

<output>
After completion, create `.planning/phases/11-multi-model-ui/11-03-SUMMARY.md`
</output>
